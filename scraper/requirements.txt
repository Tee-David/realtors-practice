# Web framework
fastapi==0.115.12
uvicorn[standard]==0.34.3

# Scraping — browser rendering + HTTP fallback
playwright==1.52.0
beautifulsoup4==4.13.4
lxml==5.4.0
curl_cffi==0.7.4

# Data processing
pydantic==2.11.3

# Queue / Job status
redis==5.2.1

# HTTP client (for LLM API calls + callbacks)
httpx==0.28.1
python-dotenv==1.1.0
